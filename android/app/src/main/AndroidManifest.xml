<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
      Required, and easy to miss: Flutter's template declares INTERNET only in
      src/debug/ and src/profile/, where it exists so the dev tooling can reach
      the VM service. Neither source set is part of a release build, so without
      this line a release APK ships with no network permission and every
      request fails while every debug build works.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--
      Lets the app tell "you are offline" apart from "the server is down", so
      a failed call can say which. Read-only; it grants no network access of
      its own.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:label="Djaber.ai"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!--
             android:screenOrientation locks rotation at the OS level, not only
             through SystemChrome.setPreferredOrientations. The Flutter call
             takes effect once the engine is running, so on a device held
             sideways the launch screen would still appear rotated and then
             snap upright.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!--
              The password-reset e-mail's link (brief §25.19). go_router maps
              /reset-password?token=… onto the reset screen.

              Opens the app by itself only once Android has verified the domain,
              which needs https://djaber.vercel.app/.well-known/assetlinks.json
              listing this package and its signing certificate. Until then,
              Android 11 and older ask which app to use; Android 12+ opens the
              browser.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="djaber.vercel.app" android:pathPrefix="/reset-password"/>
            </intent-filter>
            <!--
              The hand-off from a page already open in a phone browser: the web
              app's /reset-password page links to
              intent://app/reset-password?token=…#Intent;scheme=djaber;package=ai.djaber.djaber_mobile;end
              A custom scheme needs no domain verification, so this always
              reaches the app when it is installed.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="djaber" android:host="app" android:pathPrefix="/reset-password"/>
            </intent-filter>
            <meta-data android:name="flutter_deeplinking_enabled" android:value="true"/>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
